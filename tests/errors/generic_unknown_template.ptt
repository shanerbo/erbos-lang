// Instantiating a generic type that doesn't exist as a template
// must error rather than silently produce a missing symbol.
spark {
  // No `Foo of T` defined anywhere — must be a compile error.
  x is Foo of int()
  yell(0)
}
