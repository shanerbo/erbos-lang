use std/string
use std/map

spark {
  m is Map of String, int()
  m.set("alice", 100)
  m.set("bob", 85)
  m.set("charlie", 92)

  keys is m.keys()
  through (k in keys) {
    yell(k)
    yell(m.get(k))
  }
}
