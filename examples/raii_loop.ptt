Point is {
  x int
  y int
}

spark {
  // This loop allocates and frees 1000 times
  // Without real free, this would consume 1000*16 = 16KB
  // With free-list reuse, it stays constant
  i is 0
  infi (i lt 1000) {
    p is Point()
    p.x be i
    i be i + 1
  }
  yell(i)
  yell("raii works")
}
