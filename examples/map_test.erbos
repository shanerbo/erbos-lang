spark {
  m is map()
  map_set(m, "alice", 100)
  map_set(m, "bob", 85)
  map_set(m, "charlie", 92)

  yell(map_get(m, "alice"))
  yell(map_get(m, "bob"))
  yell(map_get(m, "charlie"))
  yell(map_len(m))
}
